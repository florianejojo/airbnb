import React, { useState, useEffect } from "react";
import {
    Button,
    Text,
    View,
    ActivityIndicator,
    StyleSheet,
    FlatList,
    Image,
    TouchableHighlight,
} from "react-native";

const RoomScreen = ({ itemId }) => {
    // console.log(route.params);
    const navigation = useNavigation();
    return <Text>RoomScreen</Text>;
};

export default RoomScreen;
